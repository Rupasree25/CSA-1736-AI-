import numpy as 
def sigmoid(x):
 return 1 / (1 + np.exp(-x))
def feedforward(X, W1, b1, W2, b2):
z1 = np.dot(X, W1) + b1
a1 = sigmoid(z1)
z2 = np.dot(a1, W2) + b2
a2 = sigmoid(z2)
 return a2
X = np.array([[0.5, 0.2]])
W1 = np.array([[0.1, 0.4, 0.2],
               [0.2, 0.3, 0.5]])
b1 = np.array([0.1, 0.1, 0.1])
W2 = np.array([[0.3],
               [0.6],
               [0.9]]
b2 = np.array([0.2])
output = feedforward(X, W1, b1, W2, b2)
print("Feedforward Output:", output)
